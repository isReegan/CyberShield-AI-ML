services:
  - type: web
    name: cybershield-ai-ml-backend
    runtime: python
    rootDir: backend
    buildCommand: |
      pip install -r requirements.txt
      python manage.py migrate
    startCommand: gunicorn cybershield.wsgi:application --bind 0.0.0.0:$PORT